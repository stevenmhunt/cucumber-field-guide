# Example 29: Running Cucumber Formatters

## Ruby

Use Cucumber flags `--format <name> --out <file | dir | url>`

## Java

Specify in the `./pom.xml` file.

## JavaScript

Use CucumberJS flags `--format <name>:<file | dir | url>`
